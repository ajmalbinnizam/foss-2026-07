# Groq API key (free tier, no HuggingFace account needed) — https://console.groq.com/keys
GROQ_API_KEY=

# Telegram bot for the live human-approval step — create one via @BotFather, then get your
# chat_id by messaging the bot once and visiting:
# https://api.telegram.org/bot<token>/getUpdates
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
